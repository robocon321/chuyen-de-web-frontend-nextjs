import { Container } from '@mui/material';
import Banner from './Banner';
import React from 'react';

const Home = (props) => {
  return (
    <main>
      <Container>
        <Banner />
      </Container>
    </main>
  )
}

export default Home;